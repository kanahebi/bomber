#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../scandal/bin/scandal" "$@"
  ret=$?
else 
  node  "$basedir/../scandal/bin/scandal" "$@"
  ret=$?
fi
exit $ret
