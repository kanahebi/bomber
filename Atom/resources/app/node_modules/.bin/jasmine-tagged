#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../jasmine-tagged/bin/jasmine-tagged" "$@"
  ret=$?
else 
  node  "$basedir/../jasmine-tagged/bin/jasmine-tagged" "$@"
  ret=$?
fi
exit $ret
