#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../ls-archive/bin/lsa" "$@"
  ret=$?
else 
  node  "$basedir/../ls-archive/bin/lsa" "$@"
  ret=$?
fi
exit $ret
