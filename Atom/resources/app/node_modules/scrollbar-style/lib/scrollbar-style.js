(function() {
  var Behavior, ScrollbarStyleObserver, behavior, observer;

  Behavior = require('emissary').Behavior;

  ScrollbarStyleObserver = require('../build/Release/scrollbar-style-observer.node').ScrollbarStyleObserver;

  observer = new ScrollbarStyleObserver(function() {
    return behavior.emitValue(observer.getPreferredScrollbarStyle());
  });

  behavior = new Behavior(observer.getPreferredScrollbarStyle());

  module.exports = behavior;

}).call(this);
