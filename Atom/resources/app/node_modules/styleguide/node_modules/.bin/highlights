#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../highlights/bin/highlights" "$@"
  ret=$?
else 
  node  "$basedir/../highlights/bin/highlights" "$@"
  ret=$?
fi
exit $ret
