#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../jasmine-focused/bin/nof" "$@"
  ret=$?
else 
  node  "$basedir/../jasmine-focused/bin/nof" "$@"
  ret=$?
fi
exit $ret
