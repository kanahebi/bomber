(function() {
  var GeneralPanel, SettingsPanel, View,
    __hasProp = {}.hasOwnProperty,
    __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; };

  View = require('atom-space-pen-views').View;

  SettingsPanel = require('./settings-panel');

  module.exports = GeneralPanel = (function(_super) {
    __extends(GeneralPanel, _super);

    function GeneralPanel() {
      return GeneralPanel.__super__.constructor.apply(this, arguments);
    }

    GeneralPanel.content = function() {
      return this.div((function(_this) {
        return function() {
          return _this.form({
            "class": 'general-panel section'
          }, function() {
            return _this.div({
              outlet: "loadingElement",
              "class": 'alert alert-info loading-area icon icon-hourglass'
            }, "Loading settings");
          });
        };
      })(this));
    };

    GeneralPanel.prototype.initialize = function() {
      this.loadingElement.remove();
      this.append(new SettingsPanel('core'));
      return this.append(new SettingsPanel('editor'));
    };

    return GeneralPanel;

  })(View);

}).call(this);
